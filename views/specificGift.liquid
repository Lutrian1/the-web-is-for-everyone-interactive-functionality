{% render 'partials/head.liquid', title: 'Homepage', logo: '<a href="/" class="Logo" aria-label="Milledoni"><img src="/images/mainLogo.png" alt="" class="M"><img src="/images/restTextLogo.png" alt="" class="illedoni"></a>' , slogan: 'Finding and sharing gift ideas' %}
 
<main>
    <nav>
        <a href="/" class="back-Button">
            <div class="back-Arrow">↵</div>
            <p>Back</p>
        </a>
    </nav>
    <img src="{{ specificGift.image }}" alt="">
    <section class="Right-Section">
        <h2>{{ specificGift.name  }}</h2>
        <form action="/save-gift" method="POST">
            <input type="hidden" name="giftId" value="{{ gift.id }}">
            <button type="submit" class="add-List-Button">+</button>
        </form>
        <dl class="show-Users">
            <img src="/images/girl1.png" alt=""><img src="/images/user1.png" alt=""><p>+{{ gift.saved }} Saved</p>
            <img src="/images/gifticon.png" alt=""><p>+{{ gift.given }} Given</p>
        </dl>
        </section>
        <section class="middle-Right-Section">
            <dl class="part1">
                <h2>Description</h2>
                <p>{{ specificGift.description }}</p>
            </dl>
            <dl class="part2">
                <img src="/images/boxquestion.png" alt="">
                <p>WHY THIS GIFT The glue is non-toxic, so it's safe to eat or drink off repaired items. To retain the gold colour it is not advised to place items in the dishwasher.</p>
            </dl>
            <dl class="part2-Bottom-Box">
                <img src="/images/chefskiss.png" alt="">
                <p>GOOD TO KNOW One kit can repair approximately eight normal plates as per the images, with 3 full cracks.</p>
            </dl>
            </div>
        </section>
        <h2>Where To Buy</h2>
        <a href="{{ specificGift.shop_url }}">
            <img src="" alt="">
            <img src="/images/shopicon.png" alt="Buy item at {{ specificGift.shop_name }}">
            <p>{{ specificGift.amount}}</p>
            <p>{{ specificGift.shop_name }}</p>
        </a>
    </div>
</main>


